import LobbyPage from "."

export const metadata = {
    title: `Memory Game Lobby | Articles Media`,
    description: "Clear the board by matching two cards one-by-one within the time limit. The player must flip the cards over to reveal an image and find a matching card hidden somewhere on the board. Only two cards can be flipped at once.",
}

export default function Home() {

  return (
    <LobbyPage />
  )

}
